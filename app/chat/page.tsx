/**
 * Strona czatu — app/chat/page.tsx
 *
 * Przekierowuje na stronę główną jeśli brak stanu zgłoszenia.
 * Stan przekazywany przez CaseProvider (z 2F.5).
 * W tej fazie (2F.4) renderuje tylko placeholder — pełne wiring w 2F.5.
 */
export default function ChatPage() {
  // Full wiring (CaseProvider + redirect) implemented in Task 2F.5.
  // Placeholder to allow Next.js build to succeed.
  return null;
}
